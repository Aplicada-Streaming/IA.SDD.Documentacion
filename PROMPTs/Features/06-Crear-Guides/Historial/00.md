# Qué hice
Ejecuté el tool-prompt Crear-Guides.md:

    1. Leí y evalué la SDD-User-Guide.md completa (1529 líneas), la regla Rule-Dual-Audience.md, los dos tool-prompts de referencia (integrador y orquestador) y el prompt de entrada PROMPT-Agente-Bootstrap-SDD.md.
    2. Verifiqué que todas las rutas del framework citadas existen (Rules, Intake, Orchestrator, bootstrap), para no afirmar nada sin evidencia.
    3. Escribí la "SDD Getting Started Guide" en la ruta pedida (el archivo estaba vacío, 2 bytes).

# Contenido de la guía
    - Resumen ejecutivo + tabla de contenido (cara humana) y frontmatter YAML con doc_id, traces, IDs de paso PASO-1..PASO-6 (cara agente), según Rule-Dual-Audience.
    
    - El modelo de tres repositorios — es la mejora metodológica del último proceso experimentado: framework (solo lectura) + destino + repositorio de documentación con PROMPTs/ e INPUTs/, que la guía de usuario base no contempla (ahí el modelo es de dos repos y el pre-intake es un chat en Claude.ai web).

    - Flujo de 6 pasos de principio a fin, con el integrador (PASO-4) y el orquestador (PASO-5) como tool-prompts reejecutables.

    - §5 explica y compara explícitamente el refinamiento vs. el modelo base, justificándolo por reproducibilidad y evidencia verificable (D9), y aclarando qué se mantiene idéntico.

    - Ejemplo aplicado real (SAI.Service.Core) con datos tomados del tool-prompt integrador (stack .NET 10/Blazor/MudBlazor/SQLite, monolítico, admin único, etapas de codificación, flujos UF-1..UF-10).

    - 3 diagramas mermaid (topología del workspace, flujo de 6 pasos, cadena de delegación tool-prompt → bootstrap → master-prompt), errores frecuentes, referencias y glosario mínimo.
    
Respeté las reglas: no inventé información (todo enlaza a la guía de usuario o a los archivos reales), no toqué el framework más que para crear el archivo destino solicitado, y no escribí en carpetas PROMPTs/ de los repos.

Nota: detecté una rareza estructural en el repo de documentación —02-Ejecutar-Prompt-Orquestador/Ejecutar-Prompt-Orquestador.md es una carpeta con un archivo del mismo nombre adentro. No lo modifiqué (es tuyo), pero quizá quieras corregirlo a un archivo plano.